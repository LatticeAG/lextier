{
  "name": "lextier",
  "version": "0.1.0",
  "description": "LexTier — deterministic per-tool approval tiers and signed audit gateway (protocol lextier/1)",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "bin": {
    "lextier": "./bin/lextier.mjs"
  },
  "exports": {
    ".": "./src/index.ts",
    "./sdk": "./src/sdk.ts",
    "./core": "./src/core/index.ts"
  },
  "scripts": {
    "test": "node --test \"tests/*.test.ts\"",
    "typecheck": "tsc --noEmit",
    "vectors": "node --test tests/vectors.test.ts",
    "smoke": "node tests/smoke.ts"
  },
  "devDependencies": {
    "@types/node": "^24.13.4",
    "typescript": "^5.9.3"
  },
  "files": [
    "src",
    "bin",
    "conformance",
    "lextier.yaml"
  ]
}
